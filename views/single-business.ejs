</section>
<section class="pt-6">
    <div class="container shadow section-sm rounded">
        <div class="columns is-multiline">
            <!-- sidebar -->
            <div class="column is-3-desktop is-12-tablet">
                <ul class="sidenav">
                    <li title="Basic Startup" class="sidelist parent active">
                        <a href="/business">Business</a>
                        <ul>
                            <% for (let index = 0; index < articles.length; index++) { %>
                            <% let article = articles[index]; %>
                            <li class="sidelist">
                                <a href="/business/<%= article.author %>">
                                    <%= article.author %>
                                </a>
                            </li>
                            <% } %>
                        </ul>
                    </li>
                </ul>
            </div>
            <!-- body -->
            <div class="column is-8-desktop is-12-tablet">
                <div class="px-4">
                    <h2 class="mb-4 has-text-weight-medium">Top Business Headlines in the US Right Now</h2>
                    <div class="content">
                        <hr>
                        <div>
                            <h2><%= business.title %></h2>
                            <h3><%= business.author %></h3>
                            <p><img src="<%= business.urlToImage %>"></p>
                            <p><strong>Published At:</strong> <%= business.publishedAt %></p>
                            <p><strong>Description:</strong> <%= business.description %></p>
                            <p><strong>Content:</strong> <%= business.content %></p>
                            <a href="<%= business.url %>" class="card-link">Read More</a>
                            <hr>
                            <form action="/savedArticles/add" method="POST">
                                <input type="hidden" name="title" value="<%= business.title %>">
                                <input type="hidden" name="author" value="<%= business.author %>">
                                <input type="hidden" name="content" value="<%= business.content %>">
                                <input type="hidden" name="description" value="<%= business.description %>">
                                <input type="hidden" name="publishedAt" value="<%= business.publishedAt %>">
                                <button type="submit" class="button is-black">Save</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
<hr>